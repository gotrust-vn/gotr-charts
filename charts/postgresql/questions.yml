questions:
- variable: container_name
  default: postgresql
  description: "Use default Container Postgresql settings."
  label: Use default Container Name
  type: string
  group: "Container Postgresql"

- variable: PATH
  default: /usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/lib/postgresql/14/bin
  description: "Use default Container Postgresql settings."
  label: PATH
  type: string
  group: "Container Postgresql"

- variable: POSTGRES_USER
  default: MasterUsr
  description: "Use default Container Postgresql settings."
  label: POSTGRES_USER
  type: string
  group: "Container Postgresql"

- variable: POSTGRES_PASSWORD
  default: Pass@PostgreSQL
  description: "Use default Container Postgresql settings."
  label: POSTGRES_PASSWORD
  type: string
  group: "Container Postgresql"

- variable: GOSU_VERSION
  default: "1.12"
  description: "Use default Container Postgresql settings."
  label: GOSU_VERSION
  type: string
  group: "Container Postgresql"

- variable: LANG
  default: en_US.utf8
  description: "Use default Container Postgresql settings."
  label: LANG
  type: string
  group: "Container Postgresql"

- variable: PG_MAJOR
  default: "14"
  description: "Use default Container Postgresql settings."
  label: PG_MAJOR
  type: string
  group: "Container Postgresql"

- variable: PG_VERSION
  default: 14.0-1.pgdg110+1
  description: "Use default Container Postgresql settings."
  label: PG_VERSION
  type: string
  group: "Container Postgresql"

- variable: PGDATA
  default: /var/lib/postgresql/data
  description: "Use default Container Postgresql settings."
  label: PGDATA
  type: string
  group: "Container Postgresql"

- variable: image.repository_psql
  default: postgres
  description: "Use default Container Postgresql settings."
  label: Image Repository Psql
  type: string
  group: "Container Postgresql"

- variable: image.tag
  default: latest
  description: "Use default Container Postgresql settings."
  label: Tag
  type: string
  group: "Container Postgresql"

- variable: image.pullPolicy
  default: Always
  description: "Use default Container Postgresql settings."
  label: Pull Policy
  type: string
  group: "Container Postgresql"