apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-identity.fullname" . }}
  labels:
    {{- include "api-identity.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "api-identity.selectorLabels" . | nindent 4 }}
