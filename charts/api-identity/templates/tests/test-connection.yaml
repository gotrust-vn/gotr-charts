apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "api-identity.fullname" . }}-test-connection"
  labels:
    {{- include "api-identity.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "api-identity.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
