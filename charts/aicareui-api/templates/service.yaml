apiVersion: v1
kind: Service
metadata:
  name: {{ include "aicareui-api.fullname" . }}
  labels:
    {{- include "aicareui-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aicareui-api.selectorLabels" . | nindent 4 }}
