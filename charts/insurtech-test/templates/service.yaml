apiVersion: v1
kind: Service
metadata:
  name: {{ include "insurtech-test.fullname" . }}
  labels:
    {{- include "insurtech-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "insurtech-test.selectorLabels" . | nindent 4 }}
